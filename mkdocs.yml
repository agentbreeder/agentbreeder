# mkdocs.yml — RETIRED
#
# The MkDocs/GitHub Pages documentation site has been retired.
# All documentation is now at https://agentbreeder.io/docs
# Served via the website/ Next.js app (deploy-website.yml workflow).
#
# This file is kept to avoid breaking any tooling that expects it.
# The docs.yml GitHub Actions workflow no longer auto-runs.

site_name: AgentBreeder (Retired — see agentbreeder.io/docs)
site_url: https://agentbreeder.io/docs
docs_dir: docs

nav:
  - Moved: README.md
