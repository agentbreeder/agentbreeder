name: Deploy Docs (Retired)
# Retired: docs are now served from agentbreeder.io via deploy-website.yml
# Trigger: manual only

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Docs retired
        run: echo "Documentation has moved to https://agentbreeder.io/docs"
