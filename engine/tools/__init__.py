"""AgentBreeder built-in tools — generic, framework-agnostic Python callables.

Tools are organised under sub-packages by source:

- ``engine.tools.standard`` — first-party tools maintained in this repo
  (web search, file writers, etc). Resolvable via ``tools/<name>`` refs.
"""
